{% extends 'base.html.twig' %}

{% block body %}
<div class="container">
	<div class="row">
		<div class="col-md-8">
				{{ dump(user) }}
			<h1>{{ user.username }}</h1>
			<p>{{ user.email }}</p>
			<p>{{ user.gender }}</p>
		</div>
	</div>
</div>
{% endblock %}
