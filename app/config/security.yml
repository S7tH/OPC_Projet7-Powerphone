security:
    providers:
        bilemo_user_provider:
            id: bilemo_user_provider

    firewalls:
        dev:
            pattern: ^/(_(profiler|wdt)|css|images|js)/
            security: false

        secured_area:
            pattern: ^/admin
            stateless: false
            simple_preauth:
                authenticator: bilemo_authenticator
            provider: bilemo_user_provider
            logout:
                path:   /admin/logout
                target: /

        main:
            anonymous: ~
        
